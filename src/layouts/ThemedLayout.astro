---
import '../styles/global.css';
import '../styles/themed-style.css';

import '@fontsource/opendyslexic';
import '@fontsource/dejavu-sans';

interface Props {
    title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, viewport-fit=cover" />
		<link rel="icon" type="image/svg+xml" href="/favicon/favicon.svg" />
		<script src="../scripts/save-theme.ts" />
		<title>{title}</title>
	</head>
	<body>

		<div class="primary">

			<aside>
				<form class="theme-selector">
					<fieldset>
						<legend class="sr-only">Choose a theme: </legend>
						
						<label for="spacetiles">
							<img src="/backgrounds/icon/SpaceTile.png" height="64" width="64" />
							<span class="theme-option-name">Space Tiles</span>
							<input type="radio" name="theme-option" id="spacetiles" checked>
						</label>

						<label for="queertiles">
							<img src="/backgrounds/icon/QueerTile.png" height="64" width="64" />
							<span class="theme-option-name">Queer Tiles</span>
							<input type="radio" name="theme-option" id="queertiles">
						</label>

					</fieldset>
				</form>

				<nav>

				</nav>
			</aside>

			<main>
				<slot />
			</main>

			<div class="side-margin" />

		</div>

	</body>
</html>

<!--


		
		<form>
			<fieldset>
				<legend>Choose a theme: </legend>
				
				<label for="spacetiles" class="visually-hidden">Space Tiles</label><input type="radio" name="themepicker" id="spacetiles" checked>

				<label for="queertiles" class="visually-hidden">Queer Tiles</label><input type="radio" name="themepicker" id="queertiles">

			</fieldset>
		</form>

		<div class="front-field">
			<slot />
		</div>

		<div class="side-spacer"></div>

-->